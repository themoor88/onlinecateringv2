%h1 Listing customers_addresses

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @addresses.each do |address|
      %tr
        %td= link_to 'Show', customers_address
        %td= link_to 'Edit', edit_customers_address_path(address)
        %td= link_to 'Destroy', customers_address, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Address', new_customers_address_path
