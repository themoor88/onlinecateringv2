= form_for @food_item, url: vendors_food_items_path(@food_item) do |f|
  - if @food_item.errors.any?
    #error_explanation
      %h2= "#{pluralize(@food_item.errors.count, "error")} prohibited this vendors_fooditem from being saved:"
      %ul
        - @food_item.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
